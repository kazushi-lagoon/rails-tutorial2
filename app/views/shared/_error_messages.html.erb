
 <!--if @user.errors.any?-->

<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(object.errors.count, "error") %>.
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <!--@userの中に、errorsという場所が用意されていて、その中に失敗した理由がrubyのオブジェクトとして格納されている。
      .full_messagesというメソッドが用意されていて、このメソッドによって、errorsの内容をユーザーに読みやすい英文（例えば、Password is too short）にして、
      配列で返してくれる。-->
     
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>